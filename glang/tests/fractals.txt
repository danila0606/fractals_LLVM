

func main() {
    int32 initRes = init();
    int32 maxIter = 1;

    int32 width = 1920;
    int32 height = 1080;

    while (is_window_opened()) {   
        int32 maxIter = maxIter + get_time_milliseconds() / 1000;

        int32 res = sdl_draw_mandelbrot(maxIter, width, height);

        int32 f_res = flush();
    }
    return 0;
}
